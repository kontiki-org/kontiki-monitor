logging:
  handlers:
    file:
      filename: /data/demo_app_service.log

# Fast heartbeats so set_degraded shows up quickly in the Registry.
# registration.group defaults to business (do not set platform here).
kontiki:
  heartbeat:
    interval: 5
