kontiki:
  http:
    address: "0.0.0.0"
    port: 8003
  registration:
    group: platform

logging:
  handlers:
    file:
      filename: /data/email_notifier_service.log

app:
  email:
    smtp:
      host: mailhog
      port: 1025
      use_starttls: false
      username: ""
      password: ""
    from:
      address: "boomerang@localhost"

  # Flat endpoint registry (endpoint_id → channel fields). Replace address for real SMTP.
  endpoints:
    oncall:
      address: ops@example.org
