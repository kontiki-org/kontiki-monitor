kontiki:
  amqp:
    url: amqp://guest:guest@rabbitmq/
  registration:
    delay: 3

logging:
  version: 1
  disable_existing_loggers: True
  formatters:
    default:
      format: "%(asctime)s - %(name)s - %(levelname)s - %(flow_id)s - %(message)s - %(filename)s:%(lineno)d"
  handlers:
    file:
      class: logging.FileHandler
      formatter: default
      level: DEBUG
  loggers:
    kontiki:
      handlers: ["file"]
      level: DEBUG
      propagate: False
  root:
    handlers: ["file"]
    level: DEBUG

