kontiki:
  http:
    address: "0.0.0.0"
    port: 8004
  registration:
    group: platform

logging:
  handlers:
    file:
      filename: /data/telegram_notifier_service.log

app:
  telegram:
    api_base_url: https://api.telegram.org
    # bot_token: provide via stack/telegram_notifier_bot_token.yaml (gitignored).
    # Optional domain icons (Boomerang >= 0.3.0): exact key, then prefix.
    category_icons:
      kontiki.registry: "⚙️"

  # Flat endpoint registry (endpoint_id → channel fields).
  endpoints:
    ops_alerts:
      chat_id: "442226941"
