FROM python:3.12-slim

WORKDIR /app

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

RUN pip install --no-cache-dir -U pip \
    && pip install --no-cache-dir "kontiki-boomerang>=0.1.0,<0.2.0"

CMD ["boomerang-subscription", "--help"]
