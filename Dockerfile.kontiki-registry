FROM python:3.12-slim

WORKDIR /app

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

RUN pip install --no-cache-dir -U pip && pip install --no-cache-dir "kontiki>=1.5.0"

CMD ["kontiki_registry", "--help"]
